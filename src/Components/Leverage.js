import React from 'react';
import '../App.css';

const Leverage = props => (
        <div className="leverage-display">
        {/*Leverage Ratios*/}
        <h4 id="leverage-title">Leverage</h4>
        <p className="leverage-ratios">Gross Profit Rate: {
        <span className="grossProfitRate"></span>
        }</p>

        <p className="leverage-ratios">Gross Profit Rate: {
        <span className="grossProfitRate"></span>
        }</p>

        <p className="leverage-ratios">Gross Profit Rate: {
        <span className="grossProfitRate"></span>
        }</p>

        <p className="leverage-ratios">Gross Profit Rate: {
        <span className="grossProfitRate"></span>
        }</p>
      </div>
  );

  export default Leverage
