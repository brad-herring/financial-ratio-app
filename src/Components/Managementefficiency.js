import React from 'react';
import '../App.css';

const Management = props => (
        <div className="management-display">
        {/*Management Efficiency Ratios*/}
        <h4 id="management-title">Management Efficiency</h4>
        <p className="management-ratios">Gross Profit Rate: {
        <span className="grossProfitRate"></span>
        }</p>

        <p className="management-ratios">Gross Profit Rate: {
        <span className="grossProfitRate"></span>
        }</p>

        <p className="management-ratios">Gross Profit Rate: {
        <span className="grossProfitRate"></span>
        }</p>

        <p className="management-ratios">Gross Profit Rate: {
        <span className="grossProfitRate"></span>
        }</p>
      </div>
  );

  export default Management
